# Example env file for backend. Copy to `backend/.env` and fill values.
# Do NOT commit `backend/.env` with real secrets.

# Groq API key (server-side only). Example: GROQ_API_KEY=sk_live_...
GROQ_API_KEY=

# Optional: override Groq API URL
GROQ_API_URL=https://api.groq.ai/v1/inference
